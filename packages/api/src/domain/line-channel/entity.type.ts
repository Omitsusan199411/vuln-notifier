export interface ReconstructedLineChannelProps {
	readonly id: string;
	notificationChannelId: string;
	lineUserId: string;
}

export interface NewLineChannelProps {
	notificationChannelId: string;
	lineUserId: string;
}
